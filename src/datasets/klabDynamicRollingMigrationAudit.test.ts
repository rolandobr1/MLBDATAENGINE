import assert from "node:assert/strict";
import { buildRolling, selectHistory, type ResultLine } from "./klabDynamicRollingExperiment";
import { discrepancyReason } from "./klabDynamicRollingMigrationAudit";
const h:ResultLine[]=[{gameId:"old",date:"2026-02-20",pitcherId:"7",started:true,source:"START",k:3,ip:"5.2",bf:21,pitches:80},{gameId:"a",date:"2026-03-01",pitcherId:"7",started:true,source:"START",k:4,ip:"6.1",bf:24,pitches:90},{gameId:"a",date:"2026-03-01",pitcherId:"7",started:true,source:"APPEARANCE",k:4,ip:"6.1",bf:24,pitches:90},{gameId:"b",date:"2026-03-03",pitcherId:"7",started:false,source:"APPEARANCE",k:1,ip:"1.0",bf:4,pitches:15},{gameId:"future",date:"2026-04-02",pitcherId:"7",started:true,source:"START",k:9,ip:"9.0",bf:30,pitches:110},{gameId:"target",date:"2026-04-01",pitcherId:"7",started:true,source:"START",k:8,ip:"8.0",bf:28,pitches:105},{gameId:"x",date:"2026-03-02",pitcherId:"8",started:true,source:"START",k:10,ip:"7.0",bf:25,pitches:100}];
const starts=selectHistory(h,"7","2026-04-01","target","START"),apps=selectHistory(h,"7","2026-04-01","target","APPEARANCE");assert.deepEqual(starts.map(x=>x.gameId),["a","old"]);assert.deepEqual(apps.map(x=>x.gameId),["b","a"]);assert.ok(!starts.some(x=>x.gameId==="target"||x.gameId==="future"));assert.ok(starts.every(x=>x.pitcherId==="7"));const f=buildRolling(starts,apps,"2026-04-01");assert.equal(f.last3StartsKs1,4);assert.equal(f.last3AppearancesKs1,1);assert.equal(f.last5StartsKsAvg,3.5);assert.equal(f.last5StartsKsStd,0.5);assert.ok(Math.abs(f.last3StartsIp1-19/3)<1e-9);assert.equal(f.last3StartsKs3,null);assert.ok(starts.some(x=>x.date<"2026-03-01"));assert.ok([...starts,...apps].every(x=>x.date<"2026-04-01"&&x.gameId!=="target"));assert.equal(new Set(starts.map(x=>`${x.gameId}:${x.pitcherId}`)).size,starts.length);assert.equal(discrepancyReason(),"UNEXPLAINED");console.log("klabDynamicRollingMigrationAudit tests passed");
